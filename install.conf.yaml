- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/work
# - ~/.vim/undo-history

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true