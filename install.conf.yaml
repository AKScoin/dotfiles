- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/work
# - ~/.vim/undo-history

- shell:
# - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"
  - sleep 2
  - 
    command: ./test.zsh
    stdout: true
    stderr: true